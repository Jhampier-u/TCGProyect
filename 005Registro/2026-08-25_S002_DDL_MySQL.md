# S002 — ADR-001 resuelta + T-006 (DDL MySQL)
**Fecha:** 2026-08-25 · **Orquestador:** Claude.md

## Requerimiento del usuario
*"Node.js, y lanza T-006"*.

## Agentes invocados
1. **Agente Arquitectura** — cierre de ADR-001, apertura de ADR-006 (ORM/migrador).
2. **Agente Base de Datos** — DDL completo (T-006) + rollback.
3. **Agente QA** — verificación ejecutando la migración contra un MySQL 8 real.
4. **Agente Documentador** — sincronización del Vault.

## Decisiones
| ID | Decisión | Estado |
|---|---|---|
| ADR-001 | **Backend en Node.js + TypeScript** | ✅ ACEPTADA (usuario) |
| ADR-006 | ORM y migrador | 🟡 ABIERTA — recomendado SQL plano + migrador ligero |

Decisión menor del Agente BD, dentro de su mandato: la collation pasa de `utf8mb4_unicode_ci`
a **`utf8mb4_0900_ai_ci`** (Unicode 9.0, más rápida y accent-insensitive: `pokemon` encuentra
`Pokémon`). Registrada en `00Master/02_Stack_Tecnologico.md`.

## Entregable
- `db/migrations/0001_initial_schema.up.sql` — 13 tablas, 20 FK, 8 CHECK.
- `db/migrations/0001_initial_schema.down.sql` — rollback en orden inverso.
- `db/README.md` — cómo aplicarlo.

## Verificación (no revisión visual: ejecución real)

No había credenciales del servidor MySQL local del usuario y Docker no tenía el demonio activo,
así que se levantó una **instancia MySQL 8.0.42 aislada y temporal** en el scratchpad
(`--initialize-insecure`, puerto 3399), se validó contra ella y se destruyó. El servicio MySQL80
del usuario **no se tocó en ningún momento**.

| Prueba | Resultado |
|---|---|
| Aplicar `up` completo | ✅ exit 0 · 13 tablas · 20 FK · 8 CHECK |
| Ciclo `up → down → up` | ✅ ambos exit 0 · 13 → 0 → 13 tablas |
| Generada `atk` con `"atk": 3000` | ✅ 3000 |
| Generada `atk` con `"atk": "?"` (Slifer) | ✅ NULL, **sin abortar el INSERT** |
| Generadas `cmc` / `lvl` / `hp` | ✅ 1.0 / 10 / 120 |
| Índice multivaluado `'G' MEMBER OF colors` | ✅ `EXPLAIN`: *Index lookup using idx_cards_mtg_colors* |
| Índice del motor de sobres | ✅ `EXPLAIN`: *Covering index lookup using idx_prints_pool* + `Using index` |
| FULLTEXT `MATCH ... AGAINST('dragon')` | ✅ 2 filas correctas |
| CHECK `distribution` no-array | ✅ rechazado (error 3819) |
| UNIQUE un solo default por (juego, set) | ✅ rechazado (error 1062) |
| UNIQUE default de juego con `set_id NULL` | ✅ rechazado — el truco de `set_key` funciona |
| Varios no-default por set | ✅ permitidos |

## Problemas
- **P-006 abierto y cerrado en la sesión**: error 1215 por FK `ON DELETE CASCADE` sobre columna
  base de una generada STORED. Aislado con caso mínimo, resuelto pasando a VIRTUAL. **Este fallo
  no era detectable leyendo el SQL** — sólo apareció al ejecutarlo.
- **P-005 abierto (🔴)**: la semilla por sí sola no reproduce una apertura si se editan las
  plantillas. Mitigado en el esquema con `pack_opening_cards` + `template_snapshot`. RN-01 se
  habría roto en silencio.

## Estado al cerrar
- H0 en curso · **H1 al 33%** (DDL hecho, faltan seeds T-007 y T-008).
- Tareas: 7 realizadas · 13 pendientes · **1 bloqueada** (era 2).
- Código: 1 migración SQL verificada. Cero código de aplicación todavía.

## Siguiente acción esperada
T-007 y T-008 (seeds de rarezas y de plantillas de sobre) — ambas desbloqueadas y sin dependencias.
