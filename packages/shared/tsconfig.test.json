{
  "$schema": "https://json.schemastore.org/tsconfig",

  // Igual que `apps/api/tsconfig.test.json`, y por lo mismo (T-086): las
  // pruebas de este paquete tampoco las comprobaba nadie.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false
  },
  "include": ["src/**/*"]
}
